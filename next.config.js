/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['cdn-icons-png.flaticon.com', 'cdn.pixabay.com','cdnb.artstation.com'],
  },
}

module.exports = nextConfig
